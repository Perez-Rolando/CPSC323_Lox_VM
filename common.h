// The common defenitions and includes for the Lox VM
#ifndef clox_common_h
#define clox_common_h

#include <cstddef>
#include <cstdint>
#include <cstdlib>

// The type aliases 
using uint8_t = std::uint8_t;
using size_t = std::size_t;

#define UINT8_COUNT (UINT8_MAX + 1)

#endif